version: '3.8'
services:
    web:
        image: mmi3docker/symfony-2024
        container_name: symfony-web-v2
        hostname: symfony-web-v2
        restart: always
        ports:
            - 8320:80
        depends_on:
            - db

        volumes:
            - ./www/:/var/www/
            - ./sites/:/etc/apache2/sites-enabled/

    db:
        image: mariadb:10.11
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: '!ChangeMe!'
            MYSQL_DATABASE: symfony
            MYSQL_USER: symfony
            MYSQL_PASSWORD: PASSWORD
        volumes:
            - db-volume:/var/lib/mysql
        ports:
            - "3306:3306"

    gotenberg:
        image: gotenberg/gotenberg:8
        restart: always
        ports:
            - "3000:3000"

    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        container_name: symfony-adminsql-v2
        hostname: symfony-adminsql-v2
        restart: always
        ports:
            - 8081:80
        environment:
            PMA_HOST: db
            MYSQL_ROOT_PASSWORD: PASSWORD
            MYSQL_USER: symfony
            MYSQL_PASSWORD: PASSWORD
            MYSQL_DATABASE: symfony    
            
    maildev:
        image: maildev/maildev
        container_name: symfony-mail-v2
        hostname: symfony-mail-v2
        command: bin/maildev --web 1080 --smtp 1025 --hide-extensions STARTTLS
        restart: always
   
        ports:
            - 1081:1080

volumes:
    db-volume: